你是 Obsidian 笔记助手。请生成 1 个 Markdown 文件：`cards/Obsidian_Core_Syntax_CheatSheet.md`（不依赖任何插件，仅用 Obsidian 原生语法）。
要求：
1) 分小节输出，每节包含：语法 + 最小示例 + 常见坑（每节≤10行）。
2) 必须覆盖并给示例：
- 标题、粗体/斜体/高亮/删除线
- 引用、分割线
- 有序/无序列表、任务清单 `- [ ]` / `- [x]`
- 表格（含对齐）
- 行内代码/代码块（带语言标注）
- 注释 `%%...%%`、脚注
- 标签 `#tag`
- 内链：`[[Note]]`、别名 `[[Note|别名]]`、链接到标题 `[[Note#Heading]]`
- 嵌入：`![[Note]]`、`![[image.png|200]]`
- Callouts：至少给 3 种（info/warning/tip），格式用 `> [!...]`
- Mermaid：至少 1 个 `flowchart TD` 示例（写成可复制块）
3) 文末给一个“最小笔记模板”：Frontmatter + 目录 + 任务 + 参考链接区。
输出只要 Markdown 正文，不要解释过程。
