按“脱离场景 + 基于项目”各生成一份（并自动挂到体系里）

你是我的 Obsidian 内容生成助手。请基于我今天/某天提供的学习记录（我会粘贴对话要点或代码改动摘要），生成并落盘以下文件；要求无插件、双链完善、模块化。

输出格式：多文件，每个文件用：
---FILE: <path>---
<markdown>

必须生成（共 4 类）：

A) 当天 Daily 复盘页：`Daily/{{YYYY-MM-DD}}.md`
- 写清：今天做了什么、测试证据、关键输出（例如并发测试结果 ok=1 fail=29）
- 链接到本次涉及的模块卡片（`[[...]]`）

B) 更新主题目录：`MOCs/JWT_MOC.md`
- 把今天新增/更新的模块卡片都加进去（`[[...]]`）
- Mermaid 图同步更新（图下必须再列一遍链接清单）

C) 生成 1 份“脱离场景”的知识卡：
- 路径：`cards/JWT_DualToken_Design.md`
- 用 `cards/_Module_Template.md` 的统一结构
- 必须覆盖：为什么单 token 不行、access vs refresh、为什么要 Redis 可撤销、logout 边界、rotation 意义、并发/原子性（概念级）

D) 生成 1 份“基于当前项目”的落地复盘卡：
- 路径：`projects/TodoDemo_JWT_DayXX_Impl.md`
- 也必须用统一结构（TL;DR/核心概念/常见追问/关联链接）
- 必须写清：涉及类（AuthService/JwtUtils/Filter/Controller）、properties 配置 key、Redis key 规则、接口输入输出、自测步骤、测试证据
- 常见追问必须“先简答一句，再给链接”。

最后：
- 在 `Index.md` 的“最近更新”区增加今天的 Daily 链接与本次两张卡片链接。
只输出多文件 Markdown，不要解释过程。
