浏览器 → DispatcherServlet → Controller → Service → Dao → MySQL

IoC：Bean 交给 Spring 管理，由 Spring 在启动时统一创建放进容器

DI：依赖注入，需要用到某个 Bean 时，由 Spring 从容器里塞给你

@Autowired：按类型注入（by type）
@Resource：优先按名字注入，找不到再按类型注入，但如果命名不对会找到错误的
